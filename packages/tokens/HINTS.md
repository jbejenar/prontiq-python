# Token Package Rules

- This package owns the frontend token contract, not the final design values.
- `P1C.00` establishes emitted artifact shape only.
- Keep build output deterministic and local to `dist/`.
- Do not add Tailwind, Mintlify, or SES integration logic here beyond placeholder artifact emission.
