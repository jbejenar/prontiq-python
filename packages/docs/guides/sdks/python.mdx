---
title: "Python SDK"
description: "Python client for the Prontiq API — coming soon."
---

<Warning>
  The Python SDK is **not yet published**. Use the [REST API](/guides/quickstart) or the [TypeScript SDK](/guides/sdks/typescript) in the meantime.
</Warning>

## Using the REST API now

```python
import requests

response = requests.get(
    "https://59jym47ia1.execute-api.ap-southeast-2.amazonaws.com/v1/address/autocomplete",
    params={"q": "16 heath cres"},
    headers={"X-Api-Key": "YOUR_API_KEY"}
)
data = response.json()
for suggestion in data["suggestions"]:
    print(f"{suggestion['addressLabel']} {suggestion['localityName']} {suggestion['state']}")
```

See the [Quickstart](/guides/quickstart) for more examples.
